import React from "react";
import "../Styles/NavBar.css";

const NavBar = () => {

    return(
        <header className='.nav'>
            <nav>
                librother.com
            </nav>
        </header>
    );
};

export default NavBar;